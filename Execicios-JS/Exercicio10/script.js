// Escreva uma função chamada 'compararStrings' que recebe duas strings como parâmetros (str1 e str2).
// Complete a função para verificar se as duas strings são idênticas.
// Se forem idênticas, a função deve retornar true, caso contrário, deve retornar false.

function compararStrings(str1, str2) {
  console.log(str1 === str2)
}

// Exemplo de uso:
const string1 = "Hello"
const string2 = "Hello"
console.log(compararStrings(string1, string2)) // Deve imprimir true ou false
